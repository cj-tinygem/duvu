---
title: "DUVU 프로젝트 전수 감사 2026-04-29"
type: doc
sources:
  - "docs/PRD.md"
related:
  - path: "knowledge/architecture/duvu-그리드-singleton-마지막-줄-규칙.md"
    rel: informed-by
confidence: medium
tags: ["audit", "project-health", "verification", "calibration"]
created: 2026-04-29
updated: 2026-04-29
---

# DUVU 프로젝트 전수 감사 2026-04-29

## 범위

외부/생성물인 `.git`, `node_modules`, `.venv`, `dist`, `build`, `coverage`, `__pycache__`, `.duvu`, `.cortex/archive`, `.cortex/.obsidian`, `demo/clones`는 제외하고 현재 워킹트리의 코드, 문서, 설정, 데이터, Cortex 메타데이터를 감사했다.

## 실행한 검증

- `ctx audit --fix`: 이슈 0, 경고 0, 자동 수정 0
- `npm test`: 통과
  - `duvu audit`: 122 통과, 0 실패
  - `token-smoke`: 통과
  - dark visual audit: 6개 뷰포트, 174개 화면/상태 통과, 85장 캡처
  - light visual audit: 6개 뷰포트, 174개 화면/상태 통과, 85장 캡처
  - link/package/security smoke: 통과
- `npm audit --audit-level=moderate`: 취약점 0
- JS 문법 검사: `bin/*.js`, `lib/*.js` 통과
- JSON 파싱 검사: `data/presets.json`, `data/defaults/presets.json`, `package-lock.json` 통과
- 데이터 무결성: presets/defaults 동일, 주요 배열 id 중복 없음

## 발견 사항

- `CALIBRATION.md`가 루트와 `docs/`에 없어 `calibrate` 기준 대조 판정은 수행할 수 없다.
- `data/references/platforms.md`에는 `fontSize: 15`, SwiftUI `size: 11/12/13/14` 등 생성 UI 16px 하한과 맞지 않는 플랫폼 예시가 남아 있다. 실제 CLI/데모 자동 게이트는 통과하지만, AI가 레퍼런스 예시를 그대로 복사하면 `tokens lint` 기준과 충돌할 수 있다.

## 판정

핵심 CLI, 토큰 엔진, 데모 렌더 감사, 패키징, 보안 게이트는 모두 통과한다. 남은 리스크는 수용 기준 파일 부재와 레퍼런스 예시의 규칙 불일치다.
